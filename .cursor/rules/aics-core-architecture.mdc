---
description: AICS 统一入口与执行架构核心规则（多入口、单内核、黑盒执行、结果导向）
alwaysApply: true
---

# AICS 核心规则（统一入口与执行架构）

## 1. 总目标（唯一核心）

所有能力，本质都是在处理「用户指令」。

无论是：写文章、分析数据、调用工具、操作电脑、调用智能体、使用记忆。

**本质统一为：** 用户输入 → 理解 → 决策 → 执行 → 返回结果。

## 2. 统一执行内核（必须统一）

以下能力 **必须** 用同一套内核处理：

- 大模型调用（AI）
- 智能体（Agent）
- 记忆（Memory）
- 模板（Template）
- 工具（Tools）
- 电脑操作（未来）

**原则：** 不允许每个模块各搞一套逻辑。

必须统一：**Controller（决策） + Execution（执行） + Memory（记忆） + Router（模型路由）**。

## 3. 入口策略

**结论：** 入口可以多个；**执行内核必须唯一**。

### 多入口（UI 层）

可以有：

1. **首页聊天入口（主入口）** — ChatGPT 式，面向普通用户。
2. **专用工具入口** — 文件处理、数据分析、模板中心、自动化等，偏结构化操作。
3. **系统级入口（未来）** — 操作电脑、调用软件、自动化任务。

**这些入口全部必须走同一个执行内核。**

## 4. 统一执行链（核心）

无论从哪个入口进来，都必须走：

**Analyze（理解） → Decide（是否执行） → Confirm（是否需要确认） → Execute（执行） → Result（结果） → Memory（沉淀）**

## 5. 用户体验规则（强约束）

### 不允许用户直接看到

- Controller / pipeline 细节
- strategist / planner / critic 等内部角色名与结构
- 输入来源（作为主导览）
- 内部执行结构
- mock/fallback 技术细节（大段铺陈）

### 必须做到

1. **输入像聊天** — 简单、自然。
2. **系统像人** — 会理解、追问、确认。
3. **执行像黑盒** — 用户侧以「正在处理…」等极简反馈为主。
4. **结果像交付** — 直接可用。

## 6. 关键架构原则

1. **入口 ≠ 执行逻辑** — 入口只是 UI；执行必须统一。
2. **所有能力都是「工具」** — AI / Agent / Memory / Tool / OS 操作，都只是 Execution Layer 的能力插件。
3. **Controller 是大脑** — 用哪个模型、调哪个 agent、用不用 memory、要不要确认、风险判断等决策集中在 Controller。
4. **Memory 是辅助，不是入口** — 记忆不应把用户拖进复杂流程；记忆在后台辅助决策。

## 7. 操作电脑与同一内核

**走同一执行内核，但可以不同入口 UI。**

- **错误：** 单独做「电脑操作系统」式产品，再搞一套完全不同的执行逻辑。
- **正确：** 用户入口可不同 → **统一 Controller** → 执行能力选择（AI / Tool / OS）→ **统一结果返回**。

用户侧仍以：正在处理… → 完成（可交付结果）为主。

## 8. 一句话架构总结

**多入口 + 单内核 + 黑盒执行 + 结果导向。**

## 9. 落地要求（持续）

1. **固化规则** — 后续开发不得违背上述架构与体验约束。
2. **推动落地**
   - 首页：**ChatGPT 式**主入口（对话驱动 + 确认执行）。
   - **各模块接入统一执行链**，禁止旁路自成一体的「小执行引擎」。

## 10. 产品定位（共识）

本思路对应的是 **操作系统级 AI 产品** 的核心架构约束，而非零散功能堆砌。
